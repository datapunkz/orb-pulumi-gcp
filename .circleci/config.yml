version: 2.1
orbs:
  pulumi: pulumi/pulumi@1.0.1
jobs:
  build_test:
    docker:
      - image: circleci/python:3.7.2
        environment:
          PIPENV_VENV_IN_PROJECT: 'true'
    steps:
      - checkout
      - run:
          name: Install Python Dependencies
          command: |
            pipenv install
      - run:
          name: Run Tests
          command: |
            pipenv run pytest
  build_push_image:
    docker:
      - image: circleci/python:3.7.2
    steps:
      - checkout
      - setup_remote_docker:
          docker_layer_caching: false
      - run:
          name: Build and push Docker image
          command: |       
            pipenv install
            pipenv run pyinstaller -F hello_world.py
            echo 'export TAG=${CIRCLE_SHA1}' >> $BASH_ENV
            echo 'export IMAGE_NAME=orb-pulumi-gcp' >> $BASH_ENV
            source $BASH_ENV
            docker build -t $DOCKER_LOGIN/$IMAGE_NAME -t $DOCKER_LOGIN/$IMAGE_NAME:$TAG .
            echo $DOCKER_PWD | docker login -u $DOCKER_LOGIN --password-stdin
            docker push $DOCKER_LOGIN/$IMAGE_NAME
  deploy_to_gcp:
    docker:
      - image: circleci/python:3.7.2
    steps:
      - checkout
      - run:
          name: Install Google Cloud SDK and Generate GCP Credentials
          command: |
            echo 'export CLOUDSDK_PYTHON=/usr/bin/python2.7' >> ${BASH_ENV}
            echo 'export GCP_CREDS_FILE=${HOME}/project/pulumi/gcp/gke/cicd_demo_gcp_creds.json' >> ${BASH_ENV}
            source ${BASH_ENV}
            echo ${GOOGLE_CLOUD_KEYS} | base64 --decode --ignore-garbage > ${GCP_CREDS_FILE}
            curl -o gcp-cli.tar.gz https://dl.google.com/dl/cloudsdk/channels/rapid/google-cloud-sdk.tar.gz
            tar -xzvf gcp-cli.tar.gz
            ./google-cloud-sdk/install.sh  --quiet
            source ${HOME}/project/google-cloud-sdk/path.bash.inc
            gcloud auth activate-service-account --key-file ${GCP_CREDS_FILE}
      - pulumi/login:
          access-token: ${PULUMI_ACCESS_TOKEN}
      - run:
          name: Install dependecies 
          command: |
            echo 'export CLOUDSDK_PYTHON=/usr/bin/python2.7' >> ${HOME}/.bashrc
            source ~/.bashrc
            source ${HOME}/project/google-cloud-sdk/path.bash.inc
            sudo pip install --upgrade pip==18.0 && pip install --user pulumi pulumi-gcp pulumi-kubernetes
      - pulumi/update:
          stack: k8s
          working_directory: ${HOME}/project/pulumi/gcp/gke/
workflows:
  build_test_deploy:
    jobs:
      - build_test
      - build_push_image:
          requires:
            - build_test
      - deploy_to_gcp:
          requires:
          - build_push_image
